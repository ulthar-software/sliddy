export interface DeckTheme {
    colors: {
        primary: string;
        accent: string;
        background: string;
    };
    fonts: {
        primary: string;
        headings: string;
    };
    text: {
        size: string;
    };
}
